apiVersion: v1
kind: Service
metadata:
  name: frontend-auth
  namespace: gestion-projet
spec:
  type: NodePort
  selector:
    app: frontend-auth
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
